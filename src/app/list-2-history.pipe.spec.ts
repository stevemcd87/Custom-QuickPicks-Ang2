import { List2HistoryPipe } from './list-2-history.pipe';

describe('List2HistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new List2HistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
